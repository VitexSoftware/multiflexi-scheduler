#!/bin/sh
# If no user provided script is activated in this file, you should remove this file.
set -e


case "$1" in
  trigger)
    if [ "$2" = "restart-multiflexi" ]; then
      systemctl try-restart multiflexi-scheduler.service || true
    fi
    ;;
esac

#DEBHELPER#

exit 0
